{
  "name": "HugoBlox Codespace",
  "features": {
    "ghcr.io/devcontainers/features/hugo:1": {
      "version": "0.162.0",
      "extended": true
    }
  },
  "updateContentCommand": "corepack enable && corepack prepare pnpm@10.14.0 --activate && pnpm install --frozen-lockfile --prefer-offline",
  "postCreateCommand": "node --version && pnpm --version && hugo version && go version",
  "customizations": {
    "vscode": {
      "extensions": [
        "ownable.ownable",
        "golang.Go"
      ]
    }
  },
  "mounts": [
    "source=pnpm-store,target=/home/vscode/.local/share/pnpm,type=volume"
  ],
  "remoteUser": "vscode",
  "forwardPorts": [1313],
  "portsAttributes": {
    "1313": {
      "label": "Hugo Server"
    }
  }
}
